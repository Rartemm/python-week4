# The command below means, that the variable "some_text" is equeal to (next line)
# whatever the user types inside. It does not matter, which type of variable will be written
# some_text = input("Input some text: ")

# After the variable is set, then it will be shown on the screen
# print(some_text)